extends layout

block content
  .hero-section
    h1 Roll: #{roll}
    h2 #{currentPlayer.name}, choose your question difficulty
    form(action="/game/select-question", method="POST")
      button.btn(type="submit", name="difficulty", value="easy") Easy Question
      button.btn(type="submit", name="difficulty", value="hard") Hard Question
